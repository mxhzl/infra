Inbox:
Ansible:
	- anubis installation and config @na
	- docker installation and config @na
	- caddy installation and config @na
	- playbook for forgejo @na
	- playbook for linkding @na
Terraform:
	- hook up ansible @na
Archive:
Search Definitions:
	Top Priority @search(@priority = 5 and not @done)
	High Priority @search(@priority > 3 and not @done)
	Maybe @search(@maybe)
	Next @search(@na and not @done and not project = "Archive")
